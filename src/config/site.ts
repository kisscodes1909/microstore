/**
 * Bật `true` khi sẵn sàng công khai trang danh mục / sản phẩm.
 * Khi `false`: ẩn menu & liên kết tới `/products/`, điều chỉnh CTA hero;
 * section thể loại mặt hàng vẫn hiển thị nhưng không dẫn vào trang sản phẩm.
 */
export const showProductsSection = false;

/** Bật khi section Dịch vụ (`#services`) được bật trên trang chủ. */
export const showServicesSection = false;
